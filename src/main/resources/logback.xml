<!-- jposapp logback configuration -->

<configuration>

	<jmxConfigurator />

	<!-- ===================== -->
	<!-- Appenders definitions -->
	<!-- ===================== -->
<timestamp key="bySecond" datePattern="yyyyMMdd'T'HHmmss"/>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- encoders are assigned the type
         ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
      <pattern>%date %-4relative [%thread] %-5level %logger{35} - %msg%n%ex{full}</pattern>
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <!-- use the previously created timestamp to create a uniquely
         named log file -->
    <file>${HOME:-${USERPROFILE}}/logs/log-${bySecond}.txt</file>
    <encoder>
       <pattern>%date %-4relative [%thread] %-5level %logger{35} - %msg%n%ex{full}</pattern>
    </encoder>
  </appender>


	<!-- ======= -->
	<!-- Loggers -->
	<!-- ======= -->
	<logger name="com.job" level="DEBUG" additivity="false">
		<appender-ref ref="STDOUT" />
	</logger>
	
	<logger name="org.springframework.test" level="INFO" additivity="false">
	  <appender-ref ref="STDOUT" />
	</logger>
	
	<logger name="org.hibernate" level="WARN">
	  	<appender-ref ref="STDOUT" />
	 </logger>
	
	
	<logger name="org.springframework.orm.jpa.JpaTransactionManager" >
	   <appender-ref ref="STDOUT" />
	</logger>

	<!-- Any other messages will be routed to the 'others' appender -->
	<root level="WARN">
		<appender-ref ref="STDOUT" />
	</root>

</configuration>